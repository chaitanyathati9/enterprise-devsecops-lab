name: Test Artifact

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Create File
        run: |
          echo "Hello World" > hello.txt

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: hello-file
          path: hello.txt